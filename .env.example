# ===================================
# A股/美股智能分析系统 - 环境变量配置文件
# 复制本文件为 .env 并填入你的配置
# ===================================

# ===================================
# 自选股配置（CN_STOCK_LIST 和 US_STOCK_LIST 至少填一个）
# ===================================

# A股代码列表（逗号分隔）
# 上证：600xxx, 601xxx, 603xxx
# 深证：000xxx, 002xxx, 300xxx
CN_STOCK_LIST=600519,300750,002594

# 美股代码列表（逗号分隔，使用标准 ticker symbol）
# 示例：AAPL（苹果）, TSLA（特斯拉）, NVDA（英伟达）, MSFT（微软）
US_STOCK_LIST=AAPL,TSLA,NVDA

# Finnhub API Keyï¼ˆç¾Žè‚¡æ•°æ®æºï¼ŒæŽ¨èé…ç½®ï¼‰
# å…è´¹æ³¨å†ŒèŽ·å–: https://finnhub.io/
FINNHUB_API_KEY=your_finnhub_api_key_here

# 数据源配置
# Tushare Pro Token（可选，在 https://tushare.pro 注册获取）
TUSHARE_TOKEN=your_tushare_token_here

# ===================================
# AI 分析配置（以下两组配置至少填一组）
# ===================================
#
# 【推荐】首选使用 Gemini，在各方面都很优秀，且有免费额度。
# 在 https://aistudio.google.com/ 注册获取免费 API Key
# 特别提醒：如果是首次使用 Gemini，建议先在网页端测试 Gemini，熟悉后再使用
#
GEMINI_API_KEY=
GEMINI_MODEL=gemini-3-flash-preview
GEMINI_MODEL_FALLBACK=gemini-2.5-flash
GEMINI_REQUEST_DELAY=2.0

# 【备选】使用 OpenAI 兼容 API，支持多种国产模型。
# 仅当您不想使用 Gemini，或需要使用特定模型时才需配置，否则留空即可。
# 支持：OpenAI、DeepSeek、通义千问、Moonshot、智谱GLM 等
#
# ┌──────────────────────────────────────────────────────────────────┐
# │ 配置说明：                                                        │
# │   OPENAI_API_KEY   = 模型服务商提供的 API Key                      │
# │   OPENAI_BASE_URL  = 模型服务商的 API 地址（OpenAI原生可不填）       │
# │   OPENAI_MODEL     = 模型服务商的模型名称，默认 gpt-4o-mini。        │
# └──────────────────────────────────────────────────────────────────┘
#
# --- OpenAI 原生（只需 Key）---
# OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxx
#
# --- DeepSeek（建议使用，大陆可直连）---
# OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxx
# OPENAI_BASE_URL=https://api.deepseek.com/v1
# OPENAI_MODEL=deepseek-chat

# 新闻搜索配置（用于获取舆情信息）
# Tavily API Keys（支持多个 Key，逗号分隔）
TAVILY_API_KEYS=your_tavily_key_here
# SerpAPI Keys（支持多个 Key，逗号分隔）
SERPAPI_API_KEYS=your_serpapi_key_here

# ===================================
# 通知渠道配置（可同时配置多个，全部推送）
# ===================================
#
# 【推荐】首选使用企业微信机器人
# 获取：企业微信群 -> 群设置 -> 群机器人 -> 添加 -> 获取 Webhook 地址
#
# WECHAT_WEBHOOK_URL=https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=your_key_here
#
# 【推荐】使用飞书机器人
# 获取：飞书群 -> 群设置 -> 群机器人 -> 添加机器人 -> 自定义机器人 -> 获取 Webhook 地址
#
# FEISHU_WEBHOOK_URL=https://open.feishu.cn/open-apis/bot/v2/hook/your_key_here
#
# 【推荐】Telegram 机器人（需要同时配置两项）
# 1. 打 Telegram 找 @BotFather -> /newbot -> 注册获取 Bot Token
# 2. 注册获取 Chat ID：可找 @userinfobot 获得或访问 https://api.telegram.org/bot<token>/getUpdates
#
# TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
# TELEGRAM_CHAT_ID=123456789
#
# 【推荐】邮件通知（只需 2 项配置）SMTP 自动识别
# 支持 QQ邮箱、163邮箱、Gmail、Outlook 等，任选一个邮箱
# 1. 注册获取授权码：例如QQ邮箱在设置 -> 帐号 -> POP3/SMTP服务 -> 开启 -> 注册获取授权码
# 2. 将授权码填入下方即可：
#
# EMAIL_SENDER=your_email@qq.com
# EMAIL_PASSWORD=your_email_auth_code
# EMAIL_RECEIVERS=receiver@example.com  # 可选，留空则发给自己
#
# 【推荐】自定义 Webhook（支持多个 Key，逗号分隔）
# 适用于：钉钉、Discord、Slack、Bark、自建服务等任意支持 POST JSON 的 Webhook
# 系统会自动识别，并自适应生成对应的消息格式
#
# CUSTOM_WEBHOOK_URLS=https://oapi.dingtalk.com/robot/send?access_token=xxx,https://hooks.slack.com/services/xxx

# 数据库配置
DATABASE_PATH=./data/stock_analysis.db

# === 定时任务配置 ===
# 是否启用定时任务（true/false）
SCHEDULE_ENABLED=false
# 每日推送的时间（HH:MM 格式，使用24小时制）
SCHEDULE_TIME=18:00
# 是否启用大盘复盘（true/false）
MARKET_REVIEW_ENABLED=true

# 系统配置
# 日志目录
LOG_DIR=./logs
# 日志级别（可选：DEBUG/INFO/WARNING/ERROR）
LOG_LEVEL=INFO
# 并发数上限（建议不要过高以避免被封禁）
MAX_WORKERS=3
# 是否启用调试日志
DEBUG=false

